#include <iostream>
using namespace std;
int stack[5];
int top = 0;
void push(int value) 
{
    if (top == 5)
    {
        cout << "Stack Overflow!" << endl;
        return;
    }
    stack[top] = value;
    top++;
}

int pop() {
    if (top == 0) {
        cout << "Stack Underflow" << endl;
        return -1;
    }
    top--;
    return stack[top];
    
}

void display() {
    if (top == 0) {
        cout << "Stack is empty" << endl;
        return;
    }
    for (int i = top - 1; i >= 0; i--)
        cout << stack[i] << endl;
}

int main() {
    int choice, value;

    while (true) {
        cout << "1-Push"<<endl;
        cout<<"2-Pop"<<endl;
        cout<<"3.Display"<<endl;
        cout<<"4.Exit"<<endl;
        cin >> choice;

        if (choice == 1) 
        {
        cout << "Enter value to push: ";
            cin >> value;
            push(value);
        } 
        if (choice == 2)
        {
          cout << "Enter value to Pop: "<< endl;
          int popped=pop();
          if(popped!=-1)
        {
       cout<<"popped value":<<popped;
        } 
}
        if (choice == 3) {
            display();
        } 
        if (choice == 4) {
            break;
        } 
        }
    }
